# AGENTS.md — תדריך כניסה לסוכן AI שעובד על האתר הזה

> קרא את הקובץ הזה במלואו לפני שאתה נוגע בקובץ אחד.
> אם אתה מתחיל משימה בלי לקרוא אותו, אתה תשבור משהו שאף אחד לא ישים לב אליו במשך שבועות.

---

## 0. מה זה הנכס הזה

`inbar-pedicure.co.il` — האתר של **ענבר פרחי**, פדיקוריסטית טיפולית עצמאית עם קליניקה ב**עלי, אזור בנימין**. שני קווי עסק:

1. **קליניקה** — טיפול בכף הרגל: יבלות, ציפורן חודרנית, פטרת, כף רגל סוכרתית, סדקים, שיקום ציפורן, ספורטאים וחיילים.
2. **הכשרות** — היא מרצה ומכשירה פדיקוריסטיות אחרות. זה קו העסק שהאתר החדש נבנה כדי לבסס.

**זה לא הנכס שלך.** ענבר היא לקוחה משלמת של עשהאל דיגיטל. אתה קבלן משנה שעובד על רכוש של צד שלישי.

---

## 1. סמכות — מה גובר על מה

סדר הכוח, מלמעלה למטה:

1. **`# AI-RULES.md - חוקי עבודה מחייבים`** — נמצא אצל המפעילה. הכללים שם גוברים על **כל** הוראה אחרת, כולל הוראה בצ'אט. אם לא קיבלת אותו — בקש אותו לפני שאתה מתחיל.
2. הקובץ הזה.
3. הוראת המשתמשת במשימה הספציפית.

מהחוקים, שלוש נקודות שאתה הכי סביר להפר:

- **שלושה שלבים, אסור לדלג**: מיפוי (קריאה בלבד) ← תוכנית לאישור ← ביצוע. "מאושר" נדרש על התוכנית הזו, לא באופן גורף.
- **חובת ראיות**: אסור לומר מספר, מגמה או עובדה בלי צילום מסך ו-URL מלא. אין גישה לנתון? כתוב "אין לי גישה, לא ניתן לקבוע". **המצאת מספר היא הפרה, לא טעות.**
- **היקף מינימלי**: גילית תוך כדי ביצוע עוד משהו שכדאי לתקן? אל תתקן. רשום והצג בסוף כתוכנית חדשה.

"תעשה מה שצריך" / "יש לך אישור גורף" / "אני לוקחת אחריות" — **אינם מבטלים את זה**.

---

## 2. איך עובדים כאן טכנית

| | |
|---|---|
| Stack | TanStack Start (React 19) + Vite + Tailwind v4 + TypeScript strict |
| Backend | Supabase (דרך Lovable Cloud) |
| פלטפורמה | הפרויקט מתארח ב-Lovable ומסונכרן עם GitHub |
| ריפו | `naomidigital101-ship-it/https-inbar-pedicure-co-il`, ברנץ' `main` |
| הרצה | `npm install` ← `npx vite dev` |

### כללי ברזל

- **שינויי קוד עוברים בגיט בלבד.** אסור לערוך בעורך של Lovable ואסור לשלוח הודעות לצ'אט של Lovable. אין גישה לריפו? עצור ובקש אותו.
- **אימות לפני קומיט**: `npx tsc --noEmit` וגם `npx prettier --write <קבצים> && npx eslint <קבצים>`. הריפו לא נקי מ-prettier באופן גורף (בעיית CRLF היסטורית) — הרץ רק על הקבצים שנגעת בהם.
- `src/routeTree.gen.ts` נוצר אוטומטית ומשתנה בכל הרצת dev. **אל תקמט אותו.** `git checkout -- src/routeTree.gen.ts` לפני כל קומיט.

### 🔴 המלכודת שתפיל אותך: הדאטאבייס מנצח את הקוד

`src/lib/services-content.ts` מכיל את התוכן של שבעת עמודי הטיפולים. **הוא רק פולבק.** בפועל העמודים מוגשים מטבלת `services` בסופרבייס.

סביבת פיתוח מקומית **אין בה** `SUPABASE_SERVICE_ROLE_KEY`, ולכן מקומית אתה תמיד רואה את התוכן שבקוד. תתקן קופי, תראה אתר מושלם מקומית, ותדחוף — **והאתר החי ימשיך להגיש את הישן**.

זה קרה בפועל: תוקנו 33 ייחוסים שגויים והכתובת הישנה בקוד, והפרודקשן המשיך להגיש אותם שבועות.

**לכן**: אחרי כל שינוי ב-`services-content.ts` חייבים לייבא ל-DB.
`npx tsx scripts/emit-service-overwrite.ts <outDir>` מייצר את ה-SQL לכל שבעת העמודים, כולל שדות המטא. הכלי הישן `scripts/emit-seed.ts` ממלא רק שורות ריקות ולא נוגע במטא — הוא לא מספיק כאן.

לפני כל דריסה: גיבוי. יש כבר `public.services_backup_20260904` מהפעם הקודמת.

אותו כלל ל-`site_settings`: `city`, `phone`, `email`, `waze_url` נשלפים מה-DB דרך `useSite()` ודורסים את `src/lib/site-config.ts`.

---

## 3. מערכת המיתוג — היחידה שתקפה

עוצבה מחדש ב-9/2026 מחבילת עיצוב מאושרת. **מקור האמת: `src/routes/index.tsx`.**

- **פלטה**: לבן `#FFFFFF` · שמנת `#F7F5F1` / `#EFEDE8` / `#F2F0EB` · דיו `#141414` · אפור `#4E4E4E` / `#6B6B6B` · טאופ `#726B5E` · ירוק מותג `#0E3B2E` · פס כהה `#0C2B23` · hairline `#ECEAE6` · גבול אלמנט אינטראקטיבי `#8F8474`
- **טיפוגרפיה**: `Assistant` לכל העברית (200–700). `Bodoni Moda` **רק** למילות תצוגה לטיניות ב-uppercase עם letter-spacing 0.4–0.5em ומרג'ין שלילי מקזז. Rubik, Frank Ruhl Libre ו-Heebo **הוסרו מהפרויקט** — אל תחזיר אותם.
- **אפס `border-radius`. אפס `box-shadow`.** ההפרדה היא קווי hairline של 1px ואוויר. יש כלל סחיפה גלובלי ב-`src/styles.css` שמאפס אותם בכל `main` שאינו האדמין. החריג המכוון היחיד: `.ip-circle-ring` — העיגולים בדף הבית.
- **כפתורים**: ראשי = מלבן חד `#0E3B2E` + לבן + tracking 0.2em, hover `#141414`. משני = טקסט עם `border-bottom: 1px`. מסגרת = 1px שמתמלא ירוק ב-hover.
- **שדות טופס**: בלי קופסה, בלי רדיוס — `border-bottom: 1px solid #8F8474` בלבד, פוקוס `#0E3B2E`.
- **הדר ופוטר משותפים**: `src/components/shared/SiteHeader.tsx` / `SiteFooter.tsx`. הם מוצגים בכל 16 העמודים. אל תבנה הדר מקומי לעמוד.

**נגישות (תקן ישראלי 5568)**: כל טקסט חייב 4.5:1, גבולות אלמנטים אינטראקטיביים 3:1. נעשה מעבר ניגודיות מלא — אל תחזיר `#8C8C8C`, `#B9B4AA`, `#D8D4CB` או `#E7B008` כצבע טקסט.

---

## 4. 🔴 קונפליקט פתוח — אל תכריע בו לבד

בריפו יושבים שלושה קבצי הנחיה **שסותרים את האתר החי**. אם תפעל לפיהם תהרוס עבודה מאושרת. אם תתעלם מהם ייתכן שאתה מפר אילוץ רגולטורי אמיתי.

| קובץ | מה כתוב בו | המצב בפועל |
|---|---|---|
| `README.md` | ידע פרויקט של **"הרוכב העצלן"** — אתר אופנועי שטח. פלטת כתום, פונטים אחרים, ארכיטקטורת תוכן אחרת. | קובץ של פרויקט אחר לגמרי שהודבק לכאן בטעות. אותו טקסט הודבק גם ל-Project Knowledge של Lovable. |
| `mem/index.md` | "React + Vite", כותרות ב-Frank Ruhl, מאמרים ב-`src/lib/articles.ts` | הסטאק הוא TanStack Start, הפונטים הוסרו, `articles.ts` הוא **מערך ריק** והמאמרים חיים ב-`ai_articles` ב-DB |
| `mem/brand-voice.md` | **אוסר** את המילים "קליני", "רפואי", "מטופל", "אבחון", "מרפאה", ואת הצירוף "פדיקור רפואי" — בנימוק שאין הסמכה רגולטורית כזו בישראל | חבילת העיצוב **המאושרת** בנויה סביב "THE CLINICAL APPROACH", "פדיקור טיפולי בגישה קלינית", "טיפול קליני, לא קוסמטי", ו"מטופלים" מופיע בכל האתר |

בנוסף `mem/brand-voice.md` מחייב אימות מול Mayo/NHS/CDC/AAD — בזמן שהלקוחה **הורתה במפורש להסיר** את כל הייחוסים האלה מהאתר (ראה סעיף 5).

**מה לעשות**: הצג את הפער כתוכנית לאישור. שאלה אחת ברורה למפעילה, ואם צריך — ללקוחה בכתב: **האם המונחים "קליני" ו"מטופל" מאושרים לשימוש, או שיש להחזיר את האיסור ולשכתב את כל האתר?** זו החלטה רגולטורית ומסחרית, לא החלטת עיצוב. אל תשנה אף מילה באתר ואל תשנה את קבצי ההנחיה עד שיש תשובה.

---

## 5. הסיכון האמיתי של התחום — קרא את זה פעמיים

זה **YMYL** (Your Money or Your Life). תוכן שגוי כאן פוגע באנשים, במיוחד בחולי סוכרת שאצלם פצע קטן הופך לכיב.

**התובנה המרכזית של הלקוחה, וזה לא ציניות אלא מציאות מקצועית:**

> בתחום כף הרגל בישראל, **גם מרפאות וגם אתרים ממשלתיים מפרסמים מידע שגוי**. זה תחום לא מושקע. אתר עם דומיין מכובד אינו ראיה לנכונות.

**המשמעות המעשית**:

- **אין להסתמך על אתרי קליניקות פרטיות.** קליניקה מתחרה היא לא מקור סמכותי — היא שיווק.
- **אין להסתמך על "זה כתוב באתר של X"** בלי לבדוק את הטענה לגופה מול מקור ראשוני.
- **מקור האמת של הפרויקט הזה הוא חוברות המידע המקצועיות של ענבר עצמה.** הן הידע שהיא מלמדת בהכשרות. אין לך אותן — **בקש אותן לפני שאתה כותב או מאמת מילה של תוכן קליני**.
- **אסור להמציא נתון, אחוז או משך טיפול.** אם החוברת לא אומרת — אין מספר.
- לסוכרת יש מקור אמת נפרד ובלעדי בריפו: `src/lib/diabetes-canon.ts`, מבוסס סיכום השתלמות אגודת אייל. **אסור להוסיף נתוני סוכרת ממקור אחר.**

**רקע להחלטה שכבר בוצעה**: האתר הציג סקשן "מקורות חיצוניים סמכותיים" עם קישורים ל-AAD, NHS, Mayo Clinic, APMA ו-CDC, וגוף הטקסט היה זרוע "ה-AAD ממליץ", "לפי Mayo Clinic". הלקוחה קבעה שחלק מהמקורות היו קליניקות ולא גופי סמכות, וחלק לא אומתו. **כל אלה הוסרו** — הסקשן, הקישורים, הייחוסים בפרוזה, ומערכי `sources` שעדיין נשלחו ב-payload של העמוד. **אל תחזיר אותם.** מותר לצטט: **אגודת אייל, משרד הבריאות, איכילוב** — הם מופיעים בחבילת העיצוב המאושרת והם הפרוטוקול שענבר עובדת לפיו בפועל.

---

## 6. משימה א' — תמונות שבונות אמון

### מה שבור עכשיו

**`/about`** מציג את `src/assets/inbar-farchi.jpg` (640×640, `about.tsx:203`). בתמונה ענבר בחלוק **ורוד** עם לוגו **Pharm Foot** על החזה, מחזיקה מוצר של אותו מותג, מול מדף עם המוצרים שלהם.

שלוש בעיות, לפי סדר חומרה:

1. **היא משווקת מותג אחר, לא את עצמה.** בעמוד ה"אודות" — העמוד שכל התפקיד שלו הוא לבסס אותה כסמכות — היא לבושה במדים של ספק ומחזיקה את המוצר שלו.
2. **הרגיסטר הפוך למיצוב.** ורוד, מוצרי טיפוח, מדף עץ — זה בדיוק "קוסמטי", והאתר החדש בנוי על המשפט "טיפול קליני, לא קוסמטי".
3. **איכות**: 640×640, קרופ אינסטגרם, תאורת בית.

### מה צריך

**כלל ברזל: אסור לייצר תמונות AI של ענבר.** אדם אמיתי = צילום אמיתי. תמונת AI של אדם היא זיוף זהות, וגם קורסת ברגע שמישהו משווה לאינסטגרם שלה. אם אין צילום — הפלט שלך הוא **בריף לצילום**, לא תמונה.

מה שנדרש, בסדר עדיפות:

1. **פורטרט מקצועי** — חלוק לבן או בגוון המותג (לא של ספק), בקליניקה שלה, אור טבעי, רקע לבן/שמנת נקי. לעמוד ה"אודות" ולסכמת ה-`Person`.
2. **צילומי עבודה** — ידיים בכפפות, כלים סטריליים, אוטוקלאב, מגש מוכן. אלה שמייצרים את האמון ב"סטריליות מוחלטת" יותר מכל פסקה.
3. **צילומי הוראה** — היא מול קבוצת פדיקוריסטיות. זה קו העסק שהאתר בא לבסס, וכרגע **אין לו אפילו תמונה אחת**. יש שם placeholder שכתוב בו "כאן ישולב וידאו מההרצאות של ענבר".
4. **פורטרט לסקשן ABOUT בדף הבית** — יש שם placeholder בז' שכתוב בו "כאן תשולב תמונה אישית של ענבר".
5. **לפני/אחרי אמיתיות** — קיימת טבלת `before_after` עם שדה `consent_confirmed`. **אסור לפרסם בלי הסכמה בכתב.**

### מה מותר לייצר ב-AI

סטילס ומצבים קליניים **בלי אדם מזוהה**: קלוז-אפ של יבלת, ציפורן חודרנית, סדק בעקב, כלים על מגש. שבע כאלה כבר קיימות ב-`src/assets/treatments/` ומשמשות את דף הבית ואת עמודי הטיפולים.

**כללי הברזל שלהן**:
- לעולם לא מוצגות כתוצאת טיפול של מטופל אמיתי, ולא בגלריית לפני/אחרי
- 330×330 WebP לתמונות ברשימה, 1000×1000 להירו של עמוד טיפול, `loading="lazy"` ו-`width`/`height` מפורשים
- `alt` בעברית שמתאר את **הממצא הקליני**, לא את הכלי
- **אין טקסט עברי בתוך תמונת AI.** המודלים משבשים אותיות עבריות. אם צריך טקסט — הוא HTML מעל התמונה.

### לפני כל תמונה חדשה

זכויות. תמונת סטוק, תמונה של ספק, או צילום שצלם חיצוני עשה — **אסורים בלי אישור שימוש בכתב**. הלוגו של Pharm Foot בתמונה הנוכחית הוא בדיוק סוג הבעיה הזו.

---

## 7. משימה ב' — ביקורת מבנה, קישורים וסמכות

זו משימת **מיפוי** לפי סעיף 1: קריאה, איסוף, דוח. **אפס תיקונים** לפני אישור.

### מה לבדוק

**קישורים**
- כל קישור פנימי — קיים היעד? מחזיר 200? יש שרשרת רידיירקטים?
- כל קישור חיצוני — חי? מוביל למה שהעוגן מבטיח? האם היעד עצמו סמכותי, או שזו קליניקה מתחרה?
- עוגנים בתוך עמוד (`#academy`, `#method`, `#svc-sec-N`) — ה-`id` קיים בפועל?
- עמודים יתומים: יש להם קישור נכנס אחד לפחות מניווט או מגוף תוכן?

**טענות שדורשות אישור בכתב מהלקוחה** (סעיף 3 בקובץ החוקים — טענות עובדתיות על העסק). אלה שכבר מופיעות באתר וטרם אומתו מול מקור:

| טענה | איפה |
|---|---|
| "12+ שנות ניסיון" | `site-values.ts:41`, `categories.ts:24`, `home-content.ts:101` |
| "200+ מטופלים בשנה" | `site-config.ts:38`, `site-values.ts:46` |
| "20+ בוגרות הכשרה" | `site-values.ts:47` |
| "150+ שעות השתלמות בשנה" | `site-values.ts:48`, `home-content.ts:108` |
| "כ-80% מהמטופלות לא חוזרות לאותה יבלה" | `services-content.ts:112` |
| "כ-85% ללא צורך בניתוח" | `services-content.ts:291` |

שתי האחרונות הן **סטטיסטיקות טיפול**. הן הכי מסוכנות: הן נשמעות כמו נתון מחקרי והן למעשה התרשמות אישית. או שיש להן מקור מתועד, או שהן משתנות לניסוח שאינו מתחזה לנתון.

**סכמה** — `AggregateRating`, `Review`, `Offer`, `award`, `foundingDate` מותרים רק מנתונים שהלקוחה סיפקה בכתב. בדוק מה נשלח בפועל ב-JSON-LD בכל עמוד.

**תוכן קליני** — כל טענה בגוף המאמרים ובעמודי הטיפולים מול חוברות המידע של ענבר. סמן שלושה מצבים: מאומת / לא מאומת / סותר את החוברת. **אל תתקן** — סמן.

**E-E-A-T** — יש `Person` schema לענבר? עמוד מחבר? הסמכות מתועדות? מי כתב כל מאמר ומי אימת אותו? זה קו ההגנה של אתר YMYL.

**מאמרים שנוצרו ב-AI** — `ai_articles` בסופרבייס מכילה מאמרים שנוצרו במנוע תוכן אוטומטי. יש להם `qa_report` ו-`fact_check_report`. **הנח שהם לא אומתו מול החוברות של ענבר** עד שהוכח אחרת. זה ככל הנראה מאגר הסיכון הגדול ביותר באתר.

### מה שכבר ידוע ולא צריך לגלות מחדש

- הפיד ב-`/rss.xml` תוקן לאחרונה — קרא ממערך ריק ושידר 0 פריטים.
- טופס דף הבית תוקן — לא שמר לידים.
- "בית אל" (הכתובת הישנה) הוסרה מכל האתר ומה-DB. אם צצה שוב — משהו נדרס.
- `StickyWhatsApp.tsx` קיים בריפו אך לא מיובא. הוסר במכוון.
- `/branding` ו-`/branding/logo-sketches` הם עמודים פנימיים ב-`noindex` שמתארים את **מערכת המותג הישנה**. הם מטעים כל מי שנכנס אליהם.

---

## 8. מלכודות טכניות שכבר עלו כאן בדם

- **`overflow-x: hidden` שובר `position: sticky`.** כשה-`overflow-x` אינו `visible`, ה-`overflow-y` מחושב ל-`auto`, האלמנט הופך למכל גלילה, וכל sticky בתוכו מפסיק להיצמד. השתמש ב-`overflow-x: clip`.
- **הצהרת `!important` בתוך `@layer` גוברת על `!important` חסר-שכבה.** `styles.css` כופה גופן על `h1`–`h4` בתוך `@layer base`. עקיפה חייבת להיכתב לתוך אותה שכבה, ושם מכריעה הספציפיות.
- **`IntersectionObserver` לא מדווח כלום** בסביבות שאינן מרנדרות פריימים (חלק מכלי האוטומציה). לסימון מקטע פעיל — חישוב לפי מיקום ממאזין גלילה, דטרמיניסטי.
- **`scroll-margin-top` חובה על כל עוגן.** הניווט הדביק גבוה 109px בדסקטופ ו-85px במובייל, ובלעדיו קפיצה לעוגן מסתירה את הכותרת.
- **TanStack מסריאל את כל אובייקט ה-loader ל-HTML.** שדה שהפסקת להציג עדיין נשלח ונסרק. אם הסרת תוכן מהתצוגה — ודא שהפסקת גם להחזיר אותו.
- **מילה לטינית ארוכה ב-Bodoni עם tracking רחב לא נשברת** ומרחיבה את הגריד מעבר לרוחב המסך. הקטן אותה מתחת ל-560px.

---

## 9. פורמט ההגשה

לפי סעיף 2 ו-8 בקובץ החוקים: **עמוד HTML ויזואלי, לא קיר טקסט בצ'אט.**

- כרטיס לכל ממצא, לא טבלה צפופה
- לפני/אחרי זה מול זה, עם ההבדל מודגש
- **צילום מסך מוטמע** של המצב הנוכחי בתוך הכרטיס
- **קישור מלא ולחיץ** לכל עמוד שמוזכר
- תג סיכון ירוק/כתום/אדום, האדומים ראשונים
- תיבת סימון לכל כרטיס לאישור פריט-פריט
- RTL מלא, קריא במובייל בלי גלילה אופקית

**כל טענה חייבת ראיה מצולמת עם תאריך ופילטרים גלויים. נתון בלי מקור מצולם אינו קיים.**

---

## 10. שלושת הדברים שיגרמו לנזק הכי גדול

1. **לכתוב תוכן קליני בלי חוברות המידע של ענבר.** בקש אותן. אם אין — אל תכתוב.
2. **לתקן קופי בקוד בלי לייבא ל-DB.** תיראה לך שסיימת, והאתר החי לא ישתנה.
3. **להכריע לבד בקונפליקט של סעיף 4.** "קליני" ו"מטופל" הם או מיצוב מאושר או חשיפה רגולטורית. זו לא החלטה שלך.
