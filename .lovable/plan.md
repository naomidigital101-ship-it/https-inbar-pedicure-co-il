אעדכן את טעינת הפונטים כך שפונט הכותרות Frank Ruhl Libre ייטען לפני קובץ ה-CSS של האתר, כדי שמשתני העיצוב והכותרות יקבלו את הפונט בזמן הרינדור הראשוני.

מה יתוקן:
- להעביר את קישורי Google Fonts ב-`src/routes/__root.tsx` לפני `appCss`.
- להשאיר את ההגדרות ב-`src/styles.css` על Heebo לגוף ו-Frank Ruhl Libre לכותרות, בלי להוסיף `@import` חיצוני ל-CSS.
- לנקות כפילות מיותרת בכותרת ההירו: להשתמש ב-`font-heading`/משתנה הפונט בצורה עקבית אחת בלבד.
- לוודא שה-H1 בהירו יורש בפועל את Frank Ruhl Libre וששאר הטקסט נשאר Heebo.

בדיקה בסיום:
- לבדוק שהקישורים לפונטים קיימים ב-head בסדר הנכון.
- לבדוק שהכותרת בהירו משתמשת בפונט הכותרות ולא נופלת לפונט ברירת מחדל.